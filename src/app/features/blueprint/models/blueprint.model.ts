import type { Blueprint as CoreBlueprint } from '../../../core/blueprint/domain/types/blueprint/blueprint.types';

// Reuse the core Blueprint domain contract to avoid duplication.
export type Blueprint = CoreBlueprint;
