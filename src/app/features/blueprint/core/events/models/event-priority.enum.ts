/**
 * 事件優先級列舉
 *
 * 用於控制事件處理的優先順序。
 * 數字越大優先級越高。
 */
export enum EventPriority {
  /** 低優先級 (0) - 非關鍵事件 */
  LOW = 0,
  /** 正常優先級 (5) - 一般事件 */
  NORMAL = 5,
  /** 高優先級 (8) - 重要事件 */
  HIGH = 8,
  /** 關鍵優先級 (10) - 必須立即處理 */
  CRITICAL = 10
}
